{
  "name": "yt-admin-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev -w apps/admin & npm run start:dev -w apps/api",
    "dev:admin": "npm run dev -w apps/admin",
    "dev:api": "npm run start:dev -w apps/api",
    "build": "npm run build -w apps/admin && npm run build -w apps/api",
    "lint": "npm run lint -w apps/admin && npm run lint -w apps/api"
  }
}
